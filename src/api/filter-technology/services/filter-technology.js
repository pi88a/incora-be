'use strict';

/**
 * filter-technology service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::filter-technology.filter-technology');
