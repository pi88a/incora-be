'use strict';

/**
 * candidate-response service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::candidate-response.candidate-response');
